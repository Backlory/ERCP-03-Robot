// Beckhoff-only residual constants kept for YunSBot context defaults.
// Full CML motor configuration has been retired.

namespace ercp {

    extern const double CUTTER_NEGLIM = (300);
    extern const double CUTTER_POSLIM = (2000);
    extern const double CUTTER_SPEED_RATIO = (10.0);

} // namespace ercp
